<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".mvp.view.MainActivity" />
        <activity android:name=".moxy.view.MoxyMainActivity">
        </activity>
        <activity android:name=".async.view.AsyncMainActivity" />
        <activity android:name=".observer.view.ObserverActivity" />
        <activity android:name=".rx.view.RxActivity" />
        <activity android:name=".rxSingle.view.SingleActivity">
        </activity>
        <activity android:name=".gson.GsonActivity" />
        <activity android:name=".retrofit.view.RetrofitActivity">
        </activity>
        <activity android:name=".room.view.RoomActivity">
        </activity>
        <activity android:name=".di.DIActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>